/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "ihzwjc2wyz54tm8l.public.blob.vercel-storage.com",
      },
    ],
  },
};

module.exports = nextConfig;
